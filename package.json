{
  "name": "creditcardchecker-node",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "npm run compile && mocha js/**/*.spec.js",
    "test-quick": "mocha js/**/*.spec.js",
    "test-watch": "npm run compile && mocha js/**/*.spec.js -w",
    "start": "node js/app.js",
    "debug": "DEBUG=*.ts node js/app.js",
    "compile": "npm run lint && rimraf js/* && tsc --sourcemap -p ts/",
    "lint": "tslint 'ts/**/*.ts'"
  },
  "author": "James Pepplinkhouse",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.11.2",
    "node-worker-pool": "^3.0.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "mocha": "^2.4.5",
    "rimraf": "^2.5.2",
    "ts-node": "^0.7.1",
    "tslint": "^3.7.0",
    "typescript": "^1.8.9",
    "typescript-require": "^0.2.9-1"
  }
}
